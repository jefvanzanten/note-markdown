# Functional Requirements

- The shared logic from rust needs to be ported to typescript so it can be used for a web-app, electron app and possibly a mopbile app in the future
- The files in shared need to go to packages but the acrhitecture needs be optimized according to best practises (classic monorepo with shared code). 